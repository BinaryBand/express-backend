<nav class="navbar navbar-expand-lg shadow mb-3">
  <div class="container">
    {{!-- Icon --}}
    <span class="navbar-brand mb-0 h1">EXAMPLE</span>

    {{!-- Collapse toggle --}}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      {{!-- Tabs left --}}
      <ul class="nav nav-pills me-lg-auto">
        {{>tab title="Home" href="/"}}
      </ul>

      {{!-- Tabs right --}}
      <ul class="nav nav-pills">
        {{#if userId}}
          {{>tab title="Settings" href="/settings"}}
          {{>tab title="Logout" href="/sign-out"}}
        {{else}}
          {{>tab title="Sign Up" href="/sign-up"}}
          {{>tab title="Login" href="/sign-in"}}
        {{/if}}
      </ul>
    </div>
  </div>
</nav>